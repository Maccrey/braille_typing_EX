{
  "name": "braille-typing-practice",
  "version": "1.0.0",
  "description": "점자 타이핑 연습 웹 애플리케이션",
  "main": "backend/server.js",
  "scripts": {
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix backend",
    "test": "npm test --prefix backend",
    "frontend:test": "npm test --prefix frontend",
    "build": "npm ci --prefix backend && npm run db:init --prefix backend",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend"
  },
  "keywords": ["braille", "typing", "practice", "accessibility"],
  "author": "maccrey",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  }
}