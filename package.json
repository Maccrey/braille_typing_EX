{
  "name": "braille-typing-ex",
  "version": "1.0.0",
  "description": "점자 타자 연습기를 위한 정적 사이트 빌드 도구",
  "private": true,
  "scripts": {
    "dev": "npm run serve --prefix frontend",
    "test": "npm test --prefix frontend",
    "build:pages": "node scripts/build-docs.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
