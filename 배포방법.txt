===========================================
점자 타자 연습기 - GitHub Pages 자동 배포
===========================================

📌 준비물
1. Firebase 프로젝트 및 웹 앱 설정
2. GitHub 저장소 (main 브랜치 사용)
3. GitHub Pages 활성화 권한
4. Firebase 웹앱 설정 값(apiKey 등)

===========================================
1단계: Firebase 웹앱 키 준비
===========================================
1. Firebase Console → 프로젝트 설정 → "일반" 탭에서 웹앱 선택
2. `firebaseConfig` 객체에 있는 값을 복사
3. 로컬에서 `cp .env.example .env` 후 값을 붙여 넣어 개발/테스트용으로 사용
4. GitHub 저장소의 **Settings → Secrets and variables → Actions**에 아래 항목 등록
   - FIREBASE_API_KEY
   - FIREBASE_AUTH_DOMAIN
   - FIREBASE_PROJECT_ID
   - FIREBASE_STORAGE_BUCKET
   - FIREBASE_MESSAGING_SENDER_ID
   - FIREBASE_APP_ID
   - (선택) FIREBASE_MEASUREMENT_ID
   - (선택) FIREBASE_DATABASE_URL

===========================================
2단계: GitHub Actions 워크플로 확인
===========================================
1. `.github/workflows/deploy.yml`가 main 브랜치에 포함되어 있는지 확인
2. 워크플로 동작
   - `frontend` 의존성 설치
   - Secrets 값으로 `.env.firebase` 생성
   - `npm run build:pages` 실행 → `docs/` 생성
   - `actions/upload-pages-artifact`로 산출물 업로드
   - `actions/deploy-pages`가 GitHub Pages 환경으로 배포
3. 필요 시 **Actions → Deploy to GitHub Pages → Run workflow**로 수동 실행 가능

===========================================
3단계: 커스텀 도메인 설정 (선택)
===========================================
1. 루트에 `CNAME` 파일 생성 후 원하는 도메인 입력 (예: typing.maccrey.com)
2. DNS 서비스에서 해당 도메인을 `username.github.io`로 CNAME 설정
3. GitHub Pages 설정 화면에서 Custom domain 입력 후 저장

===========================================
4단계: 로컬 검증 (선택)
===========================================
1. `.env`에 Firebase 키가 들어있는지 확인
2. `npm install --prefix frontend`
3. `npm run build:pages`
4. `npx http-server docs` 또는 다른 정적 서버로 테스트
5. `docs/`는 `.gitignore`되어 있으니, 커밋 전 `git status`에서 추적되지 않는지 확인

===========================================
5단계: 배포 흐름
===========================================
1. main 브랜치에 변경 사항을 커밋/푸시
2. GitHub Actions가 자동으로 실행되어 Pages에 배포
3. GitHub Pages 설정(Repo → Settings → Pages)에서 **Source=GitHub Actions**인지 확인
4. 배포 완료 후 제공된 URL 또는 Custom domain으로 접속해 확인

===========================================
⚠️ 주의사항
===========================================
- Firebase 클라이언트 키는 공개되어도 되는 값이지만, Firestore/Storage 보안 규칙은 반드시 제한적으로 설정
- 키를 교체했다면 Firebase Console에서 기존 키를 비활성화
- GitHub Secrets 값이 비어 있으면 `.env.firebase`가 만들어지지 않아 빌드가 실패
- GitHub Pages 배포 실패 시 Actions 로그에서 오류 메시지를 확인하고 재실행

===========================================
